meta {
  name: Add a message to thread
  type: http
  seq: 3
}

post {
  url: {{host}}/runs/stream
  body: json
  auth: bearer
}

auth:bearer {
  token: {{loggedToken}}
}

body:json {
  {
    "input": [
      {
        "content": "Capital de la france, repond en francais",
        "additional_kwargs": {},
        "type": "human",
        "example": false,
        "id": "human-0.9867303882733494"
      }
    ],
    "thread_id": "{{thread_id}}"
  }
}
